<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="48dp"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="horizontal"
    android:gravity="center_vertical">
    <TextView
        android:id="@+id/tv_fm_header_local"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginLeft="15dp"
        android:text="@string/fm_local"
        android:textColor="@color/black"
        android:textSize="16sp"
        android:textStyle="bold"/>
    <TextView
        android:id="@+id/tv_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginLeft="3dp"
        android:text=""
        android:textColor="@color/black"
        android:textSize="14sp" />
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_dir_header"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="horizontal"
        app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
        />
    <TextView
        android:id="@+id/btn_select"
        android:layout_width="45dp"
        android:layout_height="30dp"
        android:textSize="13sp"
        android:gravity="center"
        android:text="@string/selectAll"
        android:textColor="@color/mainColor"
        android:src="@mipmap/ic_fm_header_list"
        android:background="@color/white"
        android:visibility="gone"
        />
    <ImageButton
        android:id="@+id/ib_grid"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:src="@mipmap/ic_fm_header_grid_u"
        android:visibility="gone"
        android:background="@color/white"/>
    <ImageButton
        android:id="@+id/ib_arrow"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:layout_marginLeft="0dp"
        android:layout_marginRight="10dp"
        android:src="@mipmap/ic_fm_header_toclose"
        android:visibility="gone"
        android:background="@color/white"/>

</LinearLayout>